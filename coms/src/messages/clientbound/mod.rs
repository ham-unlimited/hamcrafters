/// Client-bound packages during the configuration phase.
pub mod configuration;
/// Client-bound packages during the login phase.
pub mod login;
/// Client-bound packages during the status phase.
pub mod status;
